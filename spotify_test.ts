Feature('spotify');

Scenario('user auth',  ({ I }) => {
    // landing page

    // tes 1: salah ketika register

    // tes 2: benar ketika register --> buat akun

    // tes 3: salah ketika login

    // tes 4: benar ketika login --> masuk ke homepage
});
